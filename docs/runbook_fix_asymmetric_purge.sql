-- =============================================================================
-- 前回非対称パージ後始末 — 手順
-- =============================================================================
-- 対象: まだ残っている注文のうち、未紐付売上 (stock_id IS NULL) があるもの
-- 目的: 売上を未紐付に揃え、在庫の settled_at だけ外して本消込できるようにする
-- しないこと: amazon_orders 削除 / inbound_items 行削除 / order_id 解除
--
-- 実行順（エディタでファイルを開き全文コピー。チャットからは貼らない）:
--   0) runbook_fix_asymmetric_purge_0_dry_run.sql
--   1) runbook_fix_asymmetric_purge_1_unlink_sales.sql
--   2) runbook_fix_asymmetric_purge_2_clear_settled.sql
-- その後アプリで本消込 (reconcile-sales) を再実行
-- =============================================================================
